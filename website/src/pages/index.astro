---
import TerminalPage from "../components/TerminalPage.astro";
import { DEFAULT_STATE_KEY } from "../lib/terminal-states";

export const prerender = false;
---

<TerminalPage initialKey={DEFAULT_STATE_KEY} />
