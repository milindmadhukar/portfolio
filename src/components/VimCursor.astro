<span class="vim-cursor">|</span>

<style>
    .vim-cursor {
        display: inline-block;
        animation: blink 1s step-end infinite;
        color: var(--ctp-text);
    }

    @keyframes blink {
        0%,
        50% {
            opacity: 1;
        }
        50.1%,
        100% {
            opacity: 0;
        }
    }
</style>
